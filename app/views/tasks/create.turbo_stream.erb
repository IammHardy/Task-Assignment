<%= turbo_stream.prepend "tasks-table", partial: "dashboard/task_row", locals: { task: @task } %>
<%= turbo_stream.replace "task-form", partial: "tasks/form", locals: { task: Task.new, employees: User.where(role: "Employee") } %>
<%= turbo_stream.replace "task-msg" do %>
  <div class="text-green-700 font-semibold animate-fade-in">Task created successfully!</div>
<% end %>
